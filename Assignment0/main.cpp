#include<cmath>
#include<eigen3/Eigen/Core>
#include<eigen3/Eigen/Dense>
#include<iostream>

int main(){

    //// Given rotation angle, point P=(2,1,1) and vector p=(2,1,0)
    //Eigen::Vector3f P(2.0f, 1.0f, 1.0f);
    //Eigen::Vector3f p(2.0f, 1.0f, 0.0f);
    //float angle = 45.0;
    //// Rotate and move
    //Eigen::Matrix3f transform_45;
    //angle = angle / 180.0 * std::acos(-1);
    //transform_45 << std::cos(angle), -std::sin(angle), 1.0, std::sin(angle), std::cos(angle), 2.0, 0.0, 0.0, 1.0;
    //// Matrix multiply
    //P = transform_45 * P;
    //std::cout << P << std::endl;
    int a, b, c = 1;
    std::cout << a + b + c;

    return 0;
}